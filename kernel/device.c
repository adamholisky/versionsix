#include <kernel_common.h>
#include <kmemory.h>
#include <device.h>

void device_initalize( void ) {
	/* SerialDevice *s;
	TerminalDevice *t;
	Device *d;

	printf( "Serial new\n" );
	s = new SerialDevice();

	printf( "\nTerminal new\n" );
	t = new TerminalDevice();

 	printf( "\nCasting\n" );
	d = s;
	d->open();
	d = t;
	d->open(); */
}